/**
 * Check if [number] is [negative].
 * {@link https://en.wikipedia.org/wiki/Negative_number|negative}
 * @param {number} n Number.
 */
function isNegative(n) {
  return n<0;
};
module.exports = isNegative;
