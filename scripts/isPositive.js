/**
 * Check if [number] is [positive].
 * {@link https://en.wikipedia.org/wiki/Positive_real_numbers|positive}
 * @param {number} n Number.
 */
function isPositive(n) {
  return n>0;
};
module.exports = isPositive;
