/**
 * Check if [number] is [even].
 * {@link https://en.wikipedia.org/wiki/Parity_(mathematics)|even}
 * @param {number} n Number.
 */
function isEven(n) {
  return n % 2===0;
};
module.exports = isEven;
