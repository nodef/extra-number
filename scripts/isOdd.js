/**
 * Check if [number] is [odd].
 * {@link https://en.wikipedia.org/wiki/Parity_(mathematics)|odd}
 * @param {number} n Number.
 */
function isOdd(n) {
  return n % 2===1;
};
module.exports = isOdd;
